- @recipes.each_slice(3) do |recipes|
 .row
  - recipes.each do |recipe|
   .col-xs-12.col-md-4
    .recipe
     .image_wrapper
      = link_to (image_tag recipe.image_url(:small)), recipe
     %h2= link_to recipe.title, recipe



 
 

